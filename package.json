{
  "name": "project-aiesec",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n server,web \"pnpm -C server dev\" \"pnpm -C Aiesec dev\"",
    "build": "pnpm -C Aiesec build",
    "preview": "pnpm -C Aiesec preview",
    "lint": "pnpm -C Aiesec lint",

    "dev:server": "pnpm -C server dev",
    "build:server": "pnpm -C server build",
    "start:server": "pnpm -C server start"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
